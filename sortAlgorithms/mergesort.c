#include <stdio.h>
#include "../number.h"

void mergesort(int count, Number *array){
	int min = 0;
	int max = 2;
	for(;;){
		if(array[0].value > array[1].value)
	}
}

void split(int count, Number *array){
	if(count == 1) return;

	split
}
